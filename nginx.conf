server {
  listen 3000;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # SPA: forward unknown paths to index.html
  location / {
    try_files $uri $uri/ /index.html;
  }

  # Ensure correct MIME for Flutter Web (WASM when used)
  types {
    application/wasm wasm;
  }
}
